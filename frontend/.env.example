# Base URL of the Nexus-Agent FastAPI backend (backend/main.py).
VITE_API_BASE_URL=http://127.0.0.1:8000

# Must match API_AUTH_TOKEN in the backend's .env. Leave blank only if the
# backend is running with no API_AUTH_TOKEN set (open/dev mode).
VITE_API_TOKEN=
